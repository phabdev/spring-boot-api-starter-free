package com.phabdev.apistarter.common.web;

/**
 * Central place for API route constants, so paths are not scattered as string literals.
 */
public final class ApiPaths {

    public static final String API = "/api";
    public static final String TASKS = API + "/tasks";

    private ApiPaths() {
    }
}
